import { TILE_S, VISUAL_S, BG_COLORS, GLOW_COLORS } from "../data/constants.js";
import { appState } from "../game/appState.js";

export class GameScene extends Phaser.Scene {
    constructor() { super('GameScene'); }
    
    // ВСТАВЬ СЮДА ВСЕ МЕТОДЫ ИЗ СВОЕГО КЛАССА (preload, create, spawnTile и т.д.)
    // ВАЖНО: Замени внутри везде window.app на appState
}
